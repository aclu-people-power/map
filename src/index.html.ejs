<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>ACLU People Power Map</title>
  <meta name="description" content="Stuff" />
  <link rel="shortcut icon" href="<%= htmlWebpackPlugin.files.favicon %>">

  <!-- FIXME is this a CDN? Maybe fetch from someplace else  -->
  <link href='https://api.mapbox.com/mapbox.js/v3.0.1/mapbox.css' rel='stylesheet' />

  <!-- Include style sheets generated by webpack -->
  <% for (var css in htmlWebpackPlugin.files.css) { %>
  <link href="<%= htmlWebpackPlugin.files.css[css] %>" rel="stylesheet">
  <% } %>
</head>

<body>
  <div class="header">
    <!-- FIXME images alias isn't getting picked up / translated by webpack -->
    <img class="logo" src="<%= require('images/logo.png') %>" />
    <h2 class="cta">Find events near you</h2>
    <div class="toolbar">
      <div>
        <input type="text" placeholder="Search by zip code" />
        <button type="button" class="btn -primary">
          <i class="icon-search"></i>
        </button>
      </div>
      <div>
        <button type="button" class="btn">Filter events</button>
        <button type="button" class="btn">List view</button>
      </div>
    </div>
    <p id="clock">
      The time is:
    </p>
  </div>
  <div class="map" id="map"></div>

  <!-- First draw an empty map -->
  <script src="<%= htmlWebpackPlugin.files.chunks['initialize-map'].entry %>"></script>

  <!-- Then grab data dependencies -->
  <script src="https://s3.amazonaws.com/thirdbear-backups/aclu/public/action_events.js"></script>
  <script src="/us_postal_codes.js"></script>

  <!-- Then startup main application -->
  <script src="<%= htmlWebpackPlugin.files.chunks['bundle'].entry %>"></script>

</body>
</html>
